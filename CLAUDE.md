# ZTO (Zero to One)

"아무것도 없는 0에서, 앱이 세상에 나가 첫 팬을 만나는 1까지" — 1인 개발사의 출시·계정·소셜·브랜딩을 한 화면에서 다루는 **Electron 데스크톱 앱 (mac/windows)**.

**진입점 문서: `SPEC.md`** — §6(전체 비전·모듈 5개)부터 읽고, §0~5(모듈 1 상세)로 내려갈 것.

## 모듈 5개 (요약) — v1 스코프는 SPEC §7 (2026-07-21 확정)

1. **앱 출시 준비** ✅v1 **우선 착수** — 양대 스토어 앱 최초 생성·IAP·자산·제출을 위저드형 플로우로 자동화 (SPEC §0~5, 실런칭에서 검증된 부품). IAP 여부는 플로우 중 선택, 필요한 자격증명 발급까지 ZTO가 안내. 스크립트는 순수 CLI (Electron·Claude 양쪽 호출 가능)
2. **계정 인벤토리** ✅v1 — 계정(Gmail 등) 목록 + 용도·특징 메타 + 계정↔소셜 프로필 매핑(구 모듈 3 절반 흡수). **비밀번호는 OS 키체인에만** — 기기에서만, 네트워크 없이, Touch ID 관문으로 보여주고 버리기 (SPEC §7.3). ZTO 데이터 파일에 비밀번호 필드 없음
3. **소셜 매트릭스** ⏸ — 매핑 뷰는 모듈 2로 흡수, 메트릭 자동 수집은 보류
4. **브랜딩 발행** 🌱 — 계정 메타 "페르소나 노트" 필드로 씨앗만. 발행 연동 보류
5. **Postiz 연동** ⏸ 보류 — 재논의 조건: 실사용 발행 플랫폼 ≥3 또는 수동 발행 고통 체감 (SPEC §7.2)

## 디자인 원칙 (2026-07-22 Dan 승인 — 모든 UI 작업에 적용)

**레퍼런스**: Apple 암호 앱(정보 구조) + postiz.com(비주얼 톤). "쓰는 사람 입장에서 와우" — 이 계정은 뭐랑 연결돼 있는지 / 이 소셜엔 내 계정이 뭐가 있는지가 **시각적으로 즉시** 보여야 한다.

- **정보 구조 3단**: 요약(통계 타일 그리드, 클릭=필터) → 목록(패널 안 행+얇은 구분선) → 상세(행 펼침, 작업은 인라인). 카드 속 카드 금지 — 컨테이너는 하나, 내부는 행.
- **비주얼 톤**: 다크 전용, 바이올렛 그라데이션(`--gradient`) 한 방향(135°) 통일. 활성 요소만 그라데이션+글로우. 표면은 투명도 위계(배경<카드<hover<활성). 토큰은 `styles.css :root` — 색 바꿀 땐 토큰만.
- **아이콘**: simple-icons 번들(네트워크 무요청). 브랜드색 16% 틴트 타일 배경 + 원색 아이콘(원색 배경+흰 아이콘 금지). 어두운 로고(X 등)는 currentColor 폴백. 계정은 이메일 해시 그라데이션 아바타.
- **금지**: 상태 표시 이모지(❓✅❌ → 상태 칩/색 텍스트), 부재 표시 텍스트("메모 없음" → 그냥 숨김. **예외**: 사용자가 토글 등으로 명시 요청한 표시는 부재라도 띄운다 — 안 띄우면 고장으로 보임), 같은 화면에 동일 CTA 버튼 2개, 상시 노출 인라인 폼(추가 폼은 버튼 클릭 시 폼 카드로), 미선택 상태에 경고색.
- **패턴**: 빈 상태 = 점선 카드 + 안내 + CTA 하나. 양자택일 = 세그먼트 컨트롤(있음|없음). 위험/상태 = 중립·그린·앰버(빨강은 진짜 에러만).
- **버튼 위계 3단**: 주 CTA = `active`(그라데이션+글로우, 화면당 1개) > 토글 열림 = `toggled`(보라 테두리+틴트, 글로우 없음) > 일반 = 기본 테두리. 토글에 `active`를 쓰면 CTA와 위계가 충돌하므로 금지.
- **hover는 밝기만**: hover에 액센트색 금지 — 흰색 투명도만 올린다(테두리 ~0.3). 액센트 테두리는 지속 상태(toggled/선택)의 전유물. hover가 상태색을 쓰면 "눌린 건지 올려둔 건지" 구분 불가.

## 원칙

- **i18n**: UI 문자열은 전부 `src/renderer/src/i18n/` 사전(en/ko)을 통한다 — 컴포넌트에 하드코딩 금지. en이 타입 원본(`Messages = typeof en`)이라 ko에 키 누락 시 컴파일 에러. 로케일은 localStorage 저장, 사이드바에서 전환
- **dev 루프**: `src/main/`·`src/preload/` 변경은 HMR 안 됨 — dev 서버 재시작 필수 (renderer만 HMR). 안 하면 신 renderer + 구 main이 섞여 빈 화면류 버그 발생 (2026-07-22 실사례). 렌더러 최후 방어선은 ErrorBoundary

- 비가역 액션(스토어 제출·결제·발행)은 반드시 사람 컨펌 후 실행 (SPEC §3 규칙 승계)
- 비밀번호는 OS 키체인에만, ZTO 데이터 파일엔 메타데이터만 (SPEC §7.3) — 네트워크 전송 없음, 타 앱 저장 비번 읽기는 불가(OS 차단)
- 결정은 전부 이 저장소 문서에 기록한다. 대화 로그에만 존재하는 결정은 결정이 아니다.


## 현황 (2026-07-21)

- [x] 이름·형태·모듈 확정, 스펙 이관 (SPEC.md)
- [x] v1 스코프 확정 — 모듈 2(심장)+1, Postiz 보류 (SPEC §7)
- [x] Electron 뼈대 — electron-vite 5 + vite 7 + React 19 + TS. **Node 22.12 필요** (`.tool-versions`로 프로젝트 로컬 고정, asdf). `npm run dev`로 기동, IPC ping 배선 확인 완료
- [x] 모듈 1→2 연동 (2026-07-22) — 위저드 "있음" 시 소유 이메일 입력 → 계정 인벤토리(`userData/zto-accounts.json`) 자동 등록. 계정 = **ID(또는 이메일)**+메모+연결된 앱(`apps`) — 2026-07-29 정정: X·Threads처럼 핸들이 곧 ID인 서비스가 있어 식별자를 이메일로 고정하지 않는다. 이메일 전용 동작(메일 앱 자동 연결·형식 검증·도메인 오타 제안)은 값에 `@`가 있을 때만 걸린다. 저장 필드명은 `Account.email` 그대로(불투명 식별자 — 개명 시 비번·접근로그 키까지 마이그레이션이라 값어치 없음). 앱 카탈로그 = 콘솔 2종(Play Console·App Store Connect) + Postiz 지원 플랫폼 전체, 아이콘은 simple-icons 번들(네트워크 무요청). 카드별 앱 편집 가능
- [x] 모듈 2 메일 서비스 자동 연결 (2026-07-22) — 앱 카탈로그에 메일 8종(Gmail·Naver·iCloud·Outlook·Yahoo·Proton·Daum·Kakao). ID가 이메일이면 @뒤 도메인으로 해당 메일 앱을 **유저 설정 없이 강제 연결 + 항상 맨 앞** (main 읽기 시점 불변식 — `mailAppForEmail`)
- [x] 모듈 2 "기기 안내" 모드 (2026-07-22) — 앱 행마다 [Chrome]/[암호 앱] 버튼: 검색어(도메인) 클립보드 복사 + 기기 비밀번호 관리자 열기. 검색어 주입 딥링크 없음(실검증) → 복사+열기가 최선. Windows는 [Edge] 치환 예정 (SPEC §7.3)
- [x] 모듈 2 비밀번호 (2026-07-22) — (계정,앱)별 저장. 암호화 키=OS 키체인(safeStorage), 암호문=`userData/zto-secrets.json`. **2FA 필수(fail-closed)**: 보기=매번 재인증, 복사=30분 세션(화면잠금·잠자기 시 즉시 무효화), 클립보드 30초 자동 삭제. **"보안" 패널**: 처리 방식 공개 + 실시간 상태 + 즉시 잠금 + 접근 로그(로컬 500건)
- [x] 모듈 2 다듬기 2차 (2026-07-22) — 상세 카드(Apple 암호 앱식: 사용자이름·암호·웹사이트·수정일·항목별 기록+액션 필터), 계정 메모(textarea, 표시 토글 스위치), 저장됨/직접입력 구분, main 문구 로케일 동기화(Touch ID 프롬프트 영어 지원), 창 최소 크기·위치 원복
- [x] **모듈 2 개명 + 자격증명 등록 + 배포 설정 (2026-07-31)** — ① **계정 개명**(`accounts:rename`): 식별자가 **비밀번호 키(`email::appId`)와 접근 로그의 열쇠**라 계정 파일만 고치면 저장된 비밀번호가 고아가 되고 기록이 갈린다 → 셋을 한 번에 옮기고, 비밀번호가 있으면 생체 관문(저장·삭제와 같은 규칙). 중복 식별자는 막는다(합치면 어느 비밀번호가 남는지 우리가 정하게 됨). *삭제는 이미 구현돼 있었음* ② **자격증명 등록 화면**(`CredentialSetup`): 발급 안내만 있고 받은 키를 넣을 곳이 없어 ASC 키는 시트 JSON을 손으로 고쳐야 했다 → [연결]에 발급(콘솔 가이드)/등록(파일 선택) 두 갈래, **검증 통과 시에만 저장**(저장부터 하고 "연결됨"이라 쓰면 첫 조회에서 터진다). ASC 키는 전역 상태(`ascCreds`) — 자격증명은 브랜드 단위 ③ **배포**(`electron-builder.yml`): 설정이 아예 없었다. 패키징하면 조용히 망가졌을 두 곳을 고침 — `launch/scripts`는 별도 프로세스로 실행되므로 **asar 밖**(extraResources), 답안 시트는 **쓰기** 대상이라 서명된 번들 안에 두면 macOS가 막으므로 **userData**로. 경로는 `resourceDir()`/`launchScript()`로 일원화. mac = 하드닝 런타임+엔타이틀먼트(Electron JIT)+공증, win = nsis 미서명 + **README에 SmartScreen 안내**(경고는 앱 실행 전에 뜨므로 인앱은 늦다 — 다운로드 시점에 알린다)
- **페르소나 노트 = 만들지 않는다 (2026-07-31 Dan)** — SPEC §7.2가 모듈 4의 씨앗으로 잡아둔 필드였으나 필요 없다는 판단. 소셜 AI 패널의 페르소나는 **코드 안 pre-context**로 충분하고(계정별로 목소리를 갈라 저장할 만큼 계정이 나뉘지 않는다), 필요해지면 그때 `socialPersona()` 뒤에 잇는다 — 소켓은 이미 있다
- [x] **만료 게이트 (2026-08-03, SPEC-BIZ §8.6 갱신)** — 체험 만료 잠금은 **공식 빌드에만**: 렌더러는 콘텐츠 blur+오버레이([요금제 보고 구매하기]→사이트 #pricing), main은 `ai:chat`·`applyEdits`·`runIap`·`createIosVersion` 이중 방어(`official && !entitled`). 예외 2곳 — 설정(키 등록 입구), 계정 인벤토리(비밀번호 인질 금지: 열람 유지, 계정 추가만 잠금). 소셜은 네이티브 뷰가 DOM 위라 blur가 못 덮으므로 잠금 중 마운트 자체를 막는다. 공식/소스 구분은 빌드 타임 `MAIN_VITE_OFFICIAL=1`(`npm run dist:mac:official`) — 소스 빌드는 게이트 없음(LICENSE.md "직접 빌드=무료"), 설정에 "소스 빌드" 안내 + Plus 업셀만. ⚠️ 공식 릴리스를 플래그 없이 빌드하면 게이트 없는 빌드가 나간다
- [x] **공식 릴리스 파이프라인 (2026-08-03, v0.1.1로 실검증)** — `v*` 태그 푸시 → CI(`.github/workflows/release.yml`)가 게이트 켠 빌드(`MAIN_VITE_OFFICIAL=1`)·서명·공증·draft 릴리스, **Publish는 사람이**. 인증서는 vibecut 것 재활용(Developer ID는 팀 단위), secrets 5개 등록 완료. 산출물명은 버전 없이(`ZTO-arm64.dmg`) — 사이트 버튼이 `releases/latest/download` 고정 URL로 항상 최신을 가리켜 릴리스마다 사이트 수정 불필요(사이트는 Vercel CLI 수동 배포). publish 설정은 yml이 아니라 CI가 주입 — 소스 빌더의 `dist:mac`이 env 없이 돌게 (`publish: null` 필수 — 빼면 git remote로 GitHub provider 자동 추론, 실측). 자동 업데이트는 v0.1.1(서명 빌드)부터 활성, updater는 `app-update.yml` 부재 시 '비활성' 표시. **릴리스 절차 = 버전 커밋 → 태그 푸시 → draft 확인 → Publish 클릭**
- [x] **무료 사용 모델 단순화 (2026-08-03 Dan 확정, SPEC-BIZ §8.6 개정)** — 시계 = **공식 빌드 첫 실행부터 3일** ('첫 스토어 연결부터'와 pull·AI 방아쇠 전부 폐기 — 조건이 늘수록 상태 조합만 혼란, 실측). 문구는 "무료 사용이 N일 남았어요"(체험→무료 프레임). 계정 인벤토리는 영구 무료 + 화면에 상시 명시("이 화면은 언제까지나 무료"), UI에 새던 내부 참조 "(SPEC §7.3)" 제거. startTrial은 whenReady에서 1회(임포트 시점은 userData 미존재 위험)
- [x] **라이선스 전환 (2026-08-03 Dan 확정, 비가역)** — source-available → **Apache 2.0 + 공식 빌드 별도 약관**(VS Code 모델). 코드는 진짜 오픈소스(런치에서 "open source" 사용 가능 — HN 지뢰 제거), 판매는 서명 빌드 약관($5=서명·공증·자동업데이트). 상표 등록 안 함(매출 유의미/사칭 시 재검토, LAUNCH.md). 사이트 /terms·/refunds 신설(LS 승인 요건). 런치 계획·문안은 `.omniscitus/launch/`(비공개 — 전략은 SPEC-BIZ와 같은 결)
- [ ] 모듈 2 잔여 — 복구경로 필드, 소셜 프로필 심화, 암호화 백업(A안)
- [x] 모듈 1 스크립트 이관 1차 (2026-07-22) — `launch/scripts/` (google token✅실검증·otp-upsert·otp-activate, apple asc-token) + `launch/answers/` 답안 시트 포맷
- [x] 출시 위저드 v0 (2026-07-22) — ①개발자 계정 점검(Play $25·Apple $99/년, 없으면 등록 가이드+함정, 상태는 userData/zto-state.json 저장) → ②앱(시트) 선택 → ③IAP 여부 선택 → ④자격증명 자동 점검 + 미보유 시 발급 가이드(콘솔 링크 포함)
- [x] 위저드 5단계 "IAP 상품 반영" (2026-07-22) — 시트 상품 목록 표시 → 2단 컨펌 → otp-upsert/otp-activate를 main이 실행(ELECTRON_RUN_AS_NODE, 스크립트 화이트리스트) → 결과 JSON 표시. ASC 반영은 App Manager 키 발급 후
- [x] 앱 대시보드 P1 — 읽기 전용 점등 (2026-07-22, SPEC §4.5) — 기존 모드에서 앱 선택 시 Android/iOS 두 컬럼 상태 트리(`AppDashboard.tsx`). pull = Play(edits 생성→tracks·listings 읽기→삭제 + oneTimeProducts 가격) / ASC(appStoreVersions+localizations·appInfos 카테고리·등급·inAppPurchasesV2), main `launch:dashboard`. 불: 🟢 스토어 확인 / 🟡 API 없는 노드(설정·자산·클로즈드 통과)만+콘솔 링크 / ⚪ 미설정. 클로즈드 "시작"은 closed 계열 트랙(internal·beta·production 제외) 릴리스 존재로 유추. **IAP 스냅샷 히스토리** `userData/zto-iap-snapshots.json` — 내용 같으면 confirmedAt만 갱신, 다르면 새 항목. 실데이터 검증 완료(Android closed 트랙·IAP ACTIVE·iOS prepare for submission). 구 `launch:storeIap`(LiveIapStatus)는 대시보드가 흡수·제거. **기존 모드는 대시보드 중심 (Dan 확정)**: 스텝 = ①앱 선택 ②대시보드 뿐 — IAP 질문·자격증명 점검·반영 스텝은 신규 여정 전용, 기존 앱의 자격증명 가이드는 API 노드에·시트 상품 반영 실행은 IAP 노드에 매달림. 속도: 대시보드 결과 캐시(`zto-dashboard-cache.json`) — **진입 시 저장본 즉시 표시, 스토어 조회는 [새로고침]으로만**(캐시 없을 때만 자동 pull) + 토큰 메모리 캐시(Google 45분·ASC 15분). 자격증명 키 경로는 기본 숨김(경로 보기 토글). **플랫폼은 탭(Android|iOS 세그먼트), 앱은 칩** — 트리가 전체 폭 사용(`.wizard.wide` 1080px). **자산 pull 포함**: Play `edits/{id}/listings/{lang}/{imageType}`(아이콘·피처그래픽·폰 스크린샷, ⚠️ `images/` 경로 아님 — 404) / ASC appScreenshotSets(templateUrl {w}x{h}). 썸네일 클릭 → 라이트박스(좌우 슬라이드·Esc). CSP img-src에 https: 추가(스토어 CDN). **메타는 풀어서**: Play title·shortDescription / ASC name·subtitle 로케일별 행 + **"전체 보기" 모달**(별도 QA 허브 Store Status 참고) — 언어 탭(서브태그로 ko-KR↔ko 매칭) × iOS/Android 두 컬럼, 필드별 글자 수: iOS 앱이름·부제·프로모션·키워드·전체설명·릴리스노트 / Android 앱이름·짧은설명·전체설명·릴리스노트 (전부 스토어 pull — ASC는 appInfoLocalizations+최신 버전 localizations 병합, Play는 listings full+릴리스 노트 로케일별). **설정**: Play는 edits/details(기본 언어·연락처)만 읽기 가능 — 등급·데이터보안·타깃연령·카테고리 등은 **읽기 API 없음**(콘솔 확인 유지), ASC는 카테고리·등급 표시. **스냅샷 = 전 섹션(메타·자산·IAP)** `zto-store-snapshots.json`(구 zto-iap-snapshots.json 대체) — 노드별 "기록 보기"가 이력을 기간 병합해 표시. 기존 모드 헤더의 "처음으로"는 제거 — "신규 앱 출시 →" 고스트 버튼으로 대체
- [x] "앱 스토어 관리" 재구성 (2026-07-22) — 기능명 "앱 출시 준비"→**"앱 스토어 관리"**, 이게 홈. 신규/기존 모드 제거하고 **[+ 앱 추가]** 클릭 시 신규/기존 선택 인라인. 진입 즉시 첫 앱 자동 선택→대시보드. **전역 API 상태**(`launch:apiStatus`, `firstGoogleSa`/`firstAscCreds`로 계정 단위 하나씩·토큰 검증)를 타이틀 우측(`.page-head.wide` 1080px, space-between)에 config처럼 — 앱별 트리의 "API 연결됨" 노드는 중복이라 제거. 앱 아이콘: 공개 스토어 조회 실패하는 미출시 앱은 **대시보드 pull이 가져온 Play 리스팅 아이콘을 캐시**(`onPulled`→부모 listSheets 갱신), 칩 아이콘 16px(칩 높이 불변). 릴리스 노트·메타 로케일별 구조로 확장(PlayReleaseRow.notes[], MetaListing.full·promo·keywords)
- [x] **대시보드 P2 편집 프레임 (2026-07-22, 골격) — 실제 write는 다음 단계** — 재사용 대기-편집 인프라. `PendingEdit`(id=`plat:section:locale:field`)·`stage()`가 스토어 값 대비 diff를 쌓고(같아지면 제거), 메타 전체 보기 모달 필드가 인라인 편집(앰버 강조·글자수·되돌리기)→ 노드 🟡(metaDirty)→ 하단 고정 **"수정 적용하기(N건)" 바**(z 110, 모달 위)→ 2단 컨펌(빨강, SPEC §3)→ `launch:applyEdits`→ **결과 패널**(항목별 🟢/🟡). 성공분만 대기서 제거+재-pull. (당시 main 핸들러는 라우팅 골격 스텁 — **실제 write는 아래 2026-07-23 항목에서 구현**.) IAP·자산도 같은 stage() 인프라에 꽂으면 됨. 실제 앱으로 UI 루프 전체 검증
- [x] 설정 페이지 + AI provider 연동 (2026-07-23) — 사이드바 하단 "설정"(en: Settings), ZTO 서비스 config. **AI = BYO 구독**(libertas naver-blog viewer 패턴): 로컬 `claude` CLI를 spawn(`claudeInfo`가 PATH+흔한 경로 폴백, `--version` 감지), 모델 선택(Fable/Opus/Sonnet/Haiku), `ai:status`/`ai:setModel`. ChatGPT·Gemini는 "곧 지원". 언어 설정도 여기로 이동(사이드바 footer의 "IPC 정상" 개발 표시는 제거 — 오류일 때만 노출). **다음: provider 토글(구독↔API키, Claude+ChatGPT), codex(ChatGPT 구독) 감지, 키체인 키 저장** (ROADMAP #1)
- [x] **로드맵 확정 (2026-07-23, `ROADMAP.md`)** — 관통 아키텍처(AI=교체가능 두뇌 / 브라우저=ZTO 소유, Electron BrowserView+webContents.debugger CDP로 자체 소유 → L2 폼필+소셜 코파일럿 공용, 포터블 / 업데이트=자체 릴리스). **소셜 코파일럿 비전**(중앙 임베드 브라우저 x·threads 유저 직접 로그인 + 우측 멀티모달 AI 패널). 실행 순서 7단계: ①AI provider ②앱 콘텐츠 설문 위저드 ③적용 실제 API(메타) ④ZTO 브라우저 자동화 ⑤to-do board ⑥소셜 코파일럿 ⑦자동 업데이트
- [x] **P2 편집 실제 write (2026-07-23, ROADMAP #3) — `launch:applyEdits` 골격→실동작** — 플랫폼별 라우팅(`applyPlayEdits`/`applyAscEdits`). **Play(android)**: 리스팅 메타(title·short·full) → edit 생성 → 로케일별 현재 값 read+변경 필드 병합 → `listings` PUT → `:commit`(**원자적** — 하나라도 실패 시 edit DELETE로 전체 롤백). **ASC(ios)**: 리소스별 PATCH(부분 성공) — `name·subtitle`→라이브 아닌 `appInfoLocalizations`, `description·promotionalText·keywords·whatsNew`→**편집 가능 상태**(PREPARE_FOR_SUBMISSION 등, 라이브 READY_FOR_SALE 제외)의 최신 버전 `appStoreVersionLocalizations`. 로케일별 id 재해석·같은 로케일 여러 필드는 PATCH 1회로 병합. 항목별 `{ok,message}` 반환 → 렌더러가 성공분만 대기서 제거+재-pull(기존 인프라 그대로). **Android 릴리스 노트는 보류**(라이브 트랙 릴리스 수정=심사·롤아웃 위험, 콘솔 안내 반환) — iOS whatsNew는 초안 버전만 편집해 안전하므로 구현(**비대칭은 의도**). ⚠️ 라이브 앱 실검증은 안전 필드(리스팅 제목 등)로 2단 컨펌 거쳐 사람이 실행 — 무단 write 금지
- [x] **설문 N개/플랫폼 일반화 + 잔여 설문 인코딩 (2026-07-23, ROADMAP #2)** — 콘솔 전용 L2 설정 설문을 등급 1종에서 플랫폼별 다종으로. 새 질문 세트 3종(`launch/questionnaires/`): `play-data-safety`(14문)·`play-target-audience`(6문)·`asc-app-privacy`(12문) — 전부 "요지" JSON(콘솔 개정 시 파일만 갱신, 기존 `play-content-rating` 패턴 승계). main `launch:questionnaireList`가 디렉터리를 스캔해 `{id,platform,title,titleEn}` 반환 → 대시보드 설정 노드가 하드코딩 `SURVEY`(플랫폼당 1개) 대신 `SurveyButtons`로 **설문별 라벨 버튼**을 그림(완료 뱃지 유지). 프리필은 iOS 연령 등급만(스토어 read), 나머지는 read API 없어 "자동조회 불가" 안내(문구 provider-중립화 + 콘솔 딥링크: ASC 앱개인정보→`/distribution/appprivacy`). typecheck·build·JSON파싱·재기동 검증
- [x] **ZTO 자체 브라우저 뼈대 + 소셜미디어 관리 모듈 (2026-07-23, ROADMAP #4·#6)** — `src/main/browser.ts`: `WebContentsView` 임베드(Electron 43, main 소유·퍼시스턴트 세션이라 유저 로그인 유지). 렌더러가 '구멍'(surface) 사각형 보고→`browser:attach`/`setBounds`, 이탈 시 `detach`. IPC: `navigate`·`back`/`forward`/`reload`·`state`통지·`eval`(값 회수 = **reverse-sync 프리미티브**)·`cdp`(디버거 패스스루). 공유 타입 `src/shared/browser-types.ts`. 렌더러: 재사용 부품 `BrowserSurface`(툴바+구멍) → **사이드바 "소셜미디어 관리"** = `SocialPage`(BrowserSurface 좌 + `AiPanel` 우 360px). AI 패널은 active provider로 대화(resume 맥락), 헤더는 그냥 "AI"(모델 문자열 노출 안 함). 뷰 bounds가 surface만 따라가 우측 패널 안 덮임. **아키텍처 확정(ROADMAP)**: 콘솔 폼은 미러링 아닌 **reverse-sync**(브라우저가 라이브 폼 읽어 JSON으로 싱크 → 개정 자동 흡수), 브라우저는 공용 인프라. **앱스토어 관리의 브라우저 UX = 슬라이드-오버**(`browser-overlay.tsx`, "캐비닛 속 TV" 은유): 사이드바 그대로·콘텐츠 영역만 fixed로 덮으며 슬라이드, 문 열린 뒤 `BrowserSurface` 마운트(뷰 attach), 닫을 땐 역순. `BrowserOverlayProvider`(App 루트 context, `useBrowserOverlay().open(url)`, 모듈 전환 시 자동 닫힘) + 앱스토어 헤더 [브라우저] 트리거. 소셜과 같은 뷰·부품 재사용
- [x] **브라우저 로그인 실증 + 렌더링 규칙 (2026-07-27)** — Play 콘솔·ASC 실제 로그인 통과로 **로그인 A안 확정**(UA에 `Electron/43.1.1`이 실려 구글이 축소 플로우 `WebLiteSignIn`을 주지만 인증은 막지 않음 — UA 위장은 정책 회색지대라 **하지 않는다**). 임베드 뷰 규칙 2개: **캔버스는 흰색**(앱 셸 색 `#0d0d12`를 깔면 배경 안 칠하는 영역에 비쳐 어두운 글자가 사라짐 — ASC 앱 목록 실측), **페이지는 라이트 고정**(뷰 단위 CDP `Emulation.setEmulatedMedia`; `nativeTheme.themeSource`는 앱 창까지 뒤집으므로 금지). CDP 연결은 `ensureDebugger()` 공용
- [x] **Android 자산 편집 실기 검증 + 구독 조회 (2026-07-31)** — ① **자산 write 실증 완료**: 라이브 리스팅 아이콘을 *지금과 동일한 파일*로 교체해 왕복만 확인(2단 컨펌은 사람이 실행). 성공의 증거는 이미지가 아니라 **URL 변경** — 캐시 키가 CDN URL 해시라 새 imageId 발급이 그대로 드러난다(바이트가 같으니 그림으로는 구분 불가). 같은 edit의 다른 자산·메타는 안 흔들림 = `:commit` 원자성 확인. ⚠️ **읽기 캐시는 재업로드 소스가 못 된다** — Play CDN이 긴 변 512px로 줄여 주므로 피처그래픽·스크린샷은 규격 검증에서 막힌다(아이콘만 우연히 원본 그대로). 복원하려면 원본 파일이 손에 있어야 한다 ② **구독 조회 추가**: 일회성만 부르고 있어서 구독만 파는 앱이 "IAP 없음"으로 보였다(Play `oneTimeProducts`↔`subscriptions`, ASC `inAppPurchasesV2`↔`subscriptionGroups`는 **서로 다른 리소스**라 절대 안 섞인다). Play는 **기본 요금제(basePlan) 단위로 행을 편다** — 상품으로 접으면 "월간 ACTIVE·연간 DRAFT"가 사라진다. 주기는 main이 ISO 토큰(`P1M`)으로 정규화하고 사전이 번역(Play=ISO, ASC=열거형), 모르는 토큰은 원문 그대로 통과. ASC 가격은 별도 리소스라 주기까지만. `ARCHIVED`·`INACTIVE`는 앰버에서 제외(앰버 = "손이 필요하다") ③ 실사용 발견: **이름·가격·주기를 한 줄에 놓자 스토어 설정 모순이 드러났다** — 대시보드의 값이 여기 있다
- [x] **API 편집 완주 + 콘솔 핸드오프 (2026-07-31, ROADMAP #3·#5)** — 대기-편집(stage) 인프라에 남은 섹션을 전부 연결하고, **API로 안 되는 것은 가이드 브라우저로 이어붙였다**. ① **iOS 자산**: ASC 3단계(예약→바이트→`uploaded:true`+md5 확정), 기기별 세트, 순서는 `relationships/appScreenshots` PATCH로 **명시**(생성 순서 가정 안 함), 세트 없으면 생성. **알파 채널 관문** — 애플이 투명도 있는 스크린샷을 "이미지가 유효하지 않음"으로만 거부하므로 PNG colorType(4·6)으로 우리가 먼저 잡는다. 크기는 기기별 표로 보되 **모르는 기기는 건너뛴다**(표가 애플보다 먼저 낡는다). Play와 달리 **원자성 없음** → 항목별 부분 성공 ② **IAP 편집**: 로케일별 이름·설명까지만. 가격·상태·구독은 **열지 않는다** — Play는 `purchaseOptions`를 통째 덮어써 대표 지역만 읽은 상태로 쓰면 나머지 172개 지역이 지워지고(그래서 `updateMask: listings`로 좁힘), 구독은 기존 구독자 동의 절차가 따로다. **읽은 것보다 넓게 쓰지 않는다**가 경계. 경로: Play `oneTimeProducts:batchUpdate`(otp-upsert.js와 같은 `regionsVersion`), ASC는 **`/v2/inAppPurchases/{id}/inAppPurchaseLocalizations`**(v1은 404·`include=`도 안 먹힘, 실측) ③ **Android 릴리스 노트**: 통째 금지 → **트랙별 판정**(프로덕션만 콘솔로). 대기 항목에 트랙을 싣고(`noteFieldKey`) 판정 규칙은 shared에 둬 화면과 결과가 안 어긋난다. 트랙 PUT은 **병합이 아니라 교체**라 릴리스 객체를 통째로 다시 실어 보낸다(versionCodes·롤아웃 비율 유실 방지). 롤아웃 중이라 릴리스가 둘이면 **고르지 않고 넘긴다** ④ **콘솔 점프 = 모드 B**: 노드 ↗와 실패 행이 **밖의 브라우저 대신 ZTO 브라우저+AI**를 연다(안내에 "무엇을 왜 못 했는지"를 실어 보냄). **[콘솔에서 처리함]**으로 대기에서 뺄 수 있다 — 없으면 끝난 일이 영원히 대기로 남아 매번 실패한다. 경계: **콘솔 폼만 임베드**, 스토어 공개 페이지·개발자 등록·결제는 외부 브라우저 ⑤ 곁들여 **로케일 의존 버그**: "새 버전 만들어 반영" 바가 영어 정규식으로 실패를 판정해 한국어 UI에선 안 떴다 → `ApplyResult.code`로 전환. ⚠️ ①②③은 실기 미검증(라이브 스토어를 바꾸므로 사람이 2단 컨펌)
- [ ] 모듈 1 잔여(→ ROADMAP): 설정 항목 L2 점등, listings/images 스크립트, P3(버전·제출), 자산 로케일 선택, 구독/가격 편집은 의도적 제외(콘솔)
- [ ] 모듈 3/4 잔여(→ ROADMAP #4·#6): reverse-sync 엔진(콘솔 폼 읽어 설문 JSON 매핑)·폼 채우기(CDP 합성 입력)·팝업 로그인·AI 패널 페이지 컨텍스트+멀티모달
- [ ] 특정 앱 정보는 이 repo에 두지 않는다 (SPEC §7.3) — 답안 시트는 각자 로컬 생성
